# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*

# Xcode
build/
DerivedData/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata/
*.xccheckout
*.moved-aside
*.xcuserstate
*.xcscmblueprint
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/
Packages/
Package.pins
Package.resolved
*.xcodeproj/project.xcworkspace/xcuserdata
*.xcworkspace/xcuserdata

# CocoaPods (just in case)
Pods/

# Carthage
Carthage/Build/

# fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output

# IDEs
.idea/
*.swp
*.swo
*~

# Environment
.env
.env.local
.env.*.local

# Logs
*.log

# Canary run artifacts (logs + screenshots from headless simulator runs)
.canary-runs/
.gstack/

# HighNotesHarness (tests/harness) — timestamped runs + SPM build + py caches.
# The fixture label images live in assets/validation/ and ARE tracked; only the
# harness's own outputs and build products are ignored.
tests/harness/results/
tests/harness/.build/
__pycache__/
*.pyc

# Claude Code loop runtime lock (transient)
.claude/scheduled_tasks.lock

# HighNotes harness live-progress runtime state
tests/harness/.progress.json
